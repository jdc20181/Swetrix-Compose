# Nginx Reverse Proxy for Swetrix

By Default Swetrix uses Nginx, this docker compose file is for users who are not comfortable with setting up Nginx on their own.



```
cp .env.example .env
# edit .env
docker compose up -d```


