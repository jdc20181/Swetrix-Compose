# Coolify Swetrix

By default Coolify uses traefik for a reverse proxy, and since Nginx is required for Swetrix, this allows you to install with Nginx setup out of the box completely compatible with traefik. 

